# Web framework and server
fastapi>=0.115.2
uvicorn>=0.32.0

# Data validation and management
pydantic[email]>=2.9.2

# SQL support (optional, remove if not using databases)
sqlalchemy>=2.0.36

# HTTP client
httpx>=0.27.2

# Environment variables
python-dotenv>=1.0.0

# PostgreSQL driver (optional, remove if not using PostgreSQL)
psycopg2-binary>=2.9.10

# Audio processing library
pydub>=0.25.1

# Optional dependencies for FFmpeg (if running locally or on custom servers)
ffmpeg-python>=0.2.0
